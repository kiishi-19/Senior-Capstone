#!/usr/bin/env bpftrace

tracepoint:module:module_load
{
    printf("%s Module loaded: %s\n", strftime("%H:%M:%S", nsecs), str(args->name));
}

tracepoint:module:module_free
{
    printf("%s Module unloaded: %s\n", strftime("%H:%M:%S", nsecs), str(args->name));
}
